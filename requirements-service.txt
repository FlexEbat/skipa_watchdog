# Зависимости для "лёгкого" service-режима (watchdog_service.py) - без
# python-telegram-bot: используется, только когда бот вообще не нужен и
# требуется исключительно локальное логирование обнаруженных сканов
# в /var/log/skipa_watchdog/.
aiohttp>=3.9
psutil>=5.9
PyYAML>=6.0
