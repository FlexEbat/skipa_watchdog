# Единственная базовая зависимость. HTTP-запросы (списки IP, обогащение
# алертов) сделаны на чистом urllib (bot/http_client.py), мониторинг
# соединений - на чтении /proc/net/tcp напрямую (bot/monitor.py), поэтому
# aiohttp и psutil больше не нужны - меньше вес установки, меньше площадь
# для уязвимостей у транзитивных зависимостей.
PyYAML>=6.0
